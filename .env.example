# Copy to .env and fill in. CRA only exposes variables prefixed REACT_APP_.
# Everything here ends up in the browser bundle, so put only public values here (never secrets).
REACT_APP_API_URL=http://localhost:8000
REACT_APP_RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxxxx
